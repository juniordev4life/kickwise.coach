-- PHASE 2: Modell-Output pro Spiel (jedes Modell-Run separat archiviert).

-- CREATE TABLE IF NOT EXISTS `kickwise-prod.kickwise_main.predictions` (
--   match_id            STRING    NOT NULL,
--   model_version       STRING    NOT NULL,
--   run_at              TIMESTAMP NOT NULL,
--   prob_home_win       FLOAT64,
--   prob_draw           FLOAT64,
--   prob_away_win       FLOAT64,
--   expected_home_goals FLOAT64,
--   expected_away_goals FLOAT64,
--   features            JSON
-- )
-- PARTITION BY DATE(run_at)
-- CLUSTER BY match_id
-- OPTIONS (
--   description = "Match-Predictions pro Modell-Run. Mehrere Versionen werden archiviert."
-- );
